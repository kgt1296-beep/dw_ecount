name: dw_ecount
description: DW 내부 가격 관리 프로그램 (Windows)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter



  intl: ^0.19.0
  # 상태관리
  provider: ^6.1.2

  # SQLite (Windows)
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.2

  # UI
  cupertino_icons: ^1.0.6
  excel: ^4.0.2
  file_picker: ^8.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/seed.sql
